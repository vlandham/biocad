<h2 class="title"><%=h @gene_group.name %></h2>

<%= render :partial => 'genes/gene_table', :locals => {:genes => @gene_group.genes} %>

<%= link_to 'Edit', edit_gene_group_path(@gene_group) %> |
<%= link_to 'Back', gene_groups_path %>
